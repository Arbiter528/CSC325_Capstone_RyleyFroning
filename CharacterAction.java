/**
 * Functional interface for character actions
 * Used to define and execute character behaviors with lambda expressions
 */
@FunctionalInterface
public interface CharacterAction {
    void execute();
}
